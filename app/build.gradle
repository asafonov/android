apply plugin: 'com.android.application'

android {
    compileSdkVersion 33
    namespace 'org.asafonov.app'

    defaultConfig {
        applicationId "org.asafonov.app"
        minSdkVersion 29
        targetSdkVersion 35
        versionCode 1
        versionName "1.0"
    }
    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
    }
}

dependencies {
    implementation fileTree(dir: 'libs', include: ['*.jar'])
}
